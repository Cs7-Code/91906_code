[project]
name = "91906-code"
version = "0.2.0"
description = ""
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Timothy Das-Tracey", email = "22269@students.lynfield.school.nz" }
]
dependencies = [
    "faker>=40.35.0",
    "flet>=0.86.1",
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "flet-cli>=0.86.1",
    "flet-desktop>=0.86.1",
    "flet-web>=0.86.1",
    # Integration testing with `flet test` / pytest. The `test` extra brings in
    # pytest, pytest-asyncio and the screenshot-comparison dependencies.
    "flet[test]>=0.86.1",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.flet]
org = "com.mycompany"
product = "91906-code"
company = "Flet"
copyright = "Copyright (C) 2023-2026 by Flet"

[tool.flet.app]
path = "src"
